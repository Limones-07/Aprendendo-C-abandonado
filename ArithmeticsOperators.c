#include <stdio.h>

int main() {

  // arithmetics operators

  // + (adition)
  // - (subtraction)
  // * (multiplication)
  // / (division)
  // % (modulus)
  // ++ (increment)
  // -- (decrement)

  int x = 5;
  int y = 2;

  // int z = x + y;
  // int z = x - y;
  // int z = x * y;
  // float z = x / (float) y;
  // int z = x % y;

  x++;
  y--;

  printf("%d\n", y);

  return 0;
}
